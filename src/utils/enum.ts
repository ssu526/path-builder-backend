export enum Progress {
  PENDING = "pending",
  IN_PROGRESS = "in progress",
  COMPLETED = "completed",
  TERMINATED = "terminated",
}

export enum Visibility {
  PROTECTED = "protected",
  PUBLIC = "public",
}
